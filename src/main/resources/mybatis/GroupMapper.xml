<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thoughtworks.twars.mapper.GroupMapper">
    <insert id="insertGroup" parameterType="com.thoughtworks.twars.bean.Group" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `group`(name, adminId, avatar, announcement, isAnnouncePublished)
        VALUES(#{name}, #{adminId}, #{avatar}, #{announcement}, #{isAnnouncePublished})
    </insert>
</mapper>